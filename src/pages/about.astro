---
import PageHead from '@/components/PageHead.astro'
import Layout from '@/layouts/Layout.astro'
import { Icon } from 'astro-icon/components'
import Link from '@/components/Link.astro'
import { buttonVariants } from '@/components/ui/button'
---

<Layout class="max-w-3xl">
  <PageHead slot="head" title="About" />
  
  <section class="py-12">
    <div class="flex flex-col gap-8 md:flex-row md:items-start">
      <div class="shrink-0">
        <div class="relative size-32">
          <svg class="absolute inset-0 w-full h-full animate-spin-slow" viewBox="0 0 100 100">
            <circle cx="50" cy="50" r="48" fill="none" stroke="currentColor" stroke-width="1" stroke-dasharray="4 4" class="text-foreground/30" />
          </svg>
          <div class="absolute inset-2 rounded-full overflow-hidden">
            <img src="/writendquit.jpg" alt=":wq!" class="h-full w-full object-cover" />
          </div>
        </div>
      </div>
      
      <div class="flex-1">
        <h1 class="mb-6 text-4xl font-bold tracking-tight">:wq!</h1>
        
        <p class="mb-2 text-lg text-muted-foreground">[colonwq] • Security Researcher</p>
        
        <div class="mb-6 space-y-4 text-muted-foreground">
          <p>
            <span class="font-medium text-foreground">:wq!</span> (colonwq) is a <Link href="https://x.com/writendquit" external class="font-medium text-foreground underline underline-offset-2 hover:text-orange-500">Security Researcher</Link>. 
            Initially working under AI jailbreak research, reverse engineering AI systems to make them 
            helpful for security research, <Link href="https://ctftime.org/user/233116" external class="font-medium text-foreground underline underline-offset-2 hover:text-orange-500">CTF playing</Link>, and bug bounty hunting.
          </p>
          <p>
            What started as a solo journey eventually grew into a team, inviting talented players like 
            <span class="font-medium text-foreground">@ricecak3h</span> specializing in binary exploitation, 
            reverse engineering, and forensics. The team continues to expand into other key areas, 
            making an impact in the security community.
          </p>
        </div>
        
        <div class="flex flex-wrap gap-3">
          <Link 
            href="https://github.com/writendquit" 
            external
            class={buttonVariants({ variant: 'outline', size: 'sm' })}
          >
            <Icon name="lucide:github" class="mr-2 size-4" />
            GitHub
          </Link>
          <Link 
            href="https://twitter.com/writendquit" 
            external
            class={buttonVariants({ variant: 'outline', size: 'sm' })}
          >
            <Icon name="lucide:twitter" class="mr-2 size-4" />
            Twitter
          </Link>
          <Link 
            href="https://ctftime.org/team/writendquit" 
            external
            class={buttonVariants({ variant: 'outline', size: 'sm' })}
          >
            <Icon name="lucide:flag" class="mr-2 size-4" />
            CTFtime
          </Link>
          <Link 
            href="mailto:contact@colonwq.me" 
            class={buttonVariants({ variant: 'outline', size: 'sm' })}
          >
            <Icon name="lucide:mail" class="mr-2 size-4" />
            Email
          </Link>
        </div>
      </div>
    </div>
  </section>
</Layout>
